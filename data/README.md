# Data Directory

This directory contains the output files generated by the pipeline:

- `attribution_data.duckdb` — DuckDB database with all tables
- `*.parquet` — Parquet files for Power BI consumption

> ⚠️ These files are generated by the pipeline and are **not** tracked in git.
